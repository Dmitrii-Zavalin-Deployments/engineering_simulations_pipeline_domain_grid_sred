# Python Package Requirements
numpy>=1.24.0
dropbox>=11.36.2
hypothesis>=6.98.0
pytest>=7.4.0
jsonschema>=4.0.0
PyYAML>=6.0.1

# ⚠️ Note: FreeCAD is a required system dependency and must be installed separately.
# Install via apt or system package manager:
#   sudo apt-get update && sudo apt-get install -y freecad
# FreeCADCmd is used as a CLI backend for STEP parsing.



